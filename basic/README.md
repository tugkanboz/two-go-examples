# basic

A suite in a `*.twogo.mjs` file, run by the built-in two-go CLI.

```bash
npm install
npm test          # runs: two-go .  (scans this folder for *.twogo.mjs)
```

It calls a public test API, so there is nothing to set up.
