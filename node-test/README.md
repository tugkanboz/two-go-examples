# node-test

Using two-go inside the Node test runner.

```bash
npm install
npm test          # runs: node --test
```

The same style works in Jest, Vitest, and Mocha, since the assertions just throw.
